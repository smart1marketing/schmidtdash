services:
  - type: web
    name: ecwid-dashboard
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ECWID_STORE_ID
        sync: false
      - key: ECWID_API_TOKEN
        sync: false
